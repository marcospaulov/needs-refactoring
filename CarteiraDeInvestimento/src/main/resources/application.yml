spring:
  application:
    name: carteiradeinvestimentos-api
  web:
    resources:
      web-application-type: servlet
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/carteiradeinvestimentos?currentSchema=carteiradeinvestimentos
    username: root
    password: root

server:
  port: 8080